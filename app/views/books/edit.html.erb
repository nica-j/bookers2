<div class ="container">
  <div class = "row">
    <div class = "mx-auto mt-3 col-md-8">
      <h1>Editing Book</h1>

      <% if @book.errors.any? %>
        <%= @book.errors.count %>error prohibited this obj from being saved:
        <ul>
          <% @book.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      <% end %>

      <%= form_with model: @book ,url: book_path(@book.id), method: :patch do |book| %>
        <label>Title</label>
        <br><%= book.text_field :title, class: "form-control" %>
        <br><label>Opinion</label>
        <br></nr><%= book.text_area :body, class: "form-control" %>
        <br><%= book.submit 'Update Book', class: "btn btn-sm btn-success" %>
      <% end %>
      
      <%= link_to 'Show', book_path(@book.id) %> |
      <%= link_to 'Back', books_path  %>
    </div>
  </div>
</div>